version: 1.0

project:
  languages:
    - python
  workspace:
    services_dir: services

schemas:
  format: "json_schema"
  output_path: .lychee

environment:
  MY_GLOBAL_ENV: orange

# Optional: plugins allowlist/config (entry point names)
# plugins:
#   - name: "my-lychee-plugin"
#     version: ">=0.1,<1.0"
#     config:
#       some_option: true
